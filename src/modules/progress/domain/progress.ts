import {STARTER_MODULES} from "@/modules/curriculum/domain/starter-content";export const STARTER_COURSE_SLUG="va-career-starter";export const STARTER_LESSONS=STARTER_MODULES.map(({id,title})=>({id,title}));export interface LessonProgressRecord{userId:string;courseSlug:string;lessonId:string;completedAt:Date}export interface ProgressRepository{listCompletedLessons(userId:string,courseSlug:string):Promise<LessonProgressRecord[]>;completeLesson(record:LessonProgressRecord):Promise<void>}
