import type{LessonProgressRecord,ProgressRepository}from"../domain/progress";export class InMemoryProgressRepository implements ProgressRepository{records:LessonProgressRecord[]=[];async listCompletedLessons(u:string,c:string){return this.records.filter(x=>x.userId===u&&x.courseSlug===c)}async completeLesson(r:LessonProgressRecord){if(!this.records.some(x=>x.userId===r.userId&&x.courseSlug===r.courseSlug&&x.lessonId===r.lessonId))this.records.push(r)}}
