export interface QuizChoice{id:string;label:string}export interface QuizQuestion{id:string;prompt:string;choices:QuizChoice[];correctChoiceId:string;explanation:string}export interface QuizDefinition{id:string;lessonId:string;version:number;passPercent:number;questions:QuizQuestion[]}export interface QuizAttemptRecord{id:string;userId:string;quizId:string;lessonId:string;quizVersion:number;score:number;passed:boolean;answers:Record<string,string>;createdAt:Date}export interface AssessmentRepository{createAttempt(r:QuizAttemptRecord):Promise<void>;listAttemptsForUser(userId:string,quizId?:string):Promise<QuizAttemptRecord[]>}export interface QuizResult{attemptId:string;score:number;passed:boolean;correctAnswers:number;totalQuestions:number;feedback:Array<{questionId:string;selectedChoiceId:string;correctChoiceId:string;correct:boolean;explanation:string}>}
