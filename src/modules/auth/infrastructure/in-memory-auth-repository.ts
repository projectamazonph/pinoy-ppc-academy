import type {AuthRepository,SessionRecord,UserRecord} from "../domain/auth";
export class InMemoryAuthRepository implements AuthRepository{users:UserRecord[]=[];sessions:SessionRecord[]=[];async findUserByEmail(e:string){return this.users.find(u=>u.email===e)??null}async findUserById(id:string){return this.users.find(u=>u.id===id)??null}async createUser(u:UserRecord){if(await this.findUserByEmail(u.email))throw new Error("already exists");this.users.push(u)}async createSession(s:SessionRecord){this.sessions.push(s)}async findSessionByTokenHash(h:string){return this.sessions.find(s=>s.tokenHash===h)??null}async revokeSession(h:string,t:Date){const s=await this.findSessionByTokenHash(h);if(s)s.revokedAt=t}}
