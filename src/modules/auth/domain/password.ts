import { randomBytes, scrypt as cb, timingSafeEqual } from "node:crypto"; import { promisify } from "node:util";
const scrypt=promisify(cb); const COMMON=["password","password123","qwerty123","123456789","admin123","letmein"];
export function validatePassword(password:string){const e:string[]=[]; const n=password.trim().toLowerCase(); if(password.length<12)e.push("Use at least 12 characters"); if(!/[a-z]/.test(password))e.push("Add a lowercase letter"); if(!/[A-Z]/.test(password))e.push("Add an uppercase letter"); if(!/\d/.test(password))e.push("Add a number"); if(!/[^A-Za-z0-9]/.test(password))e.push("Add a symbol"); if(COMMON.some(x=>n.includes(x)))e.push("Choose a less common password"); return e}
export async function hashPassword(password:string){const salt=randomBytes(16); const key=await scrypt(password,salt,64) as Buffer; return `scrypt$${salt.toString("base64url")}$${key.toString("base64url")}`}
export async function verifyPassword(password:string,stored:string){const [a,s,h]=stored.split("$"); if(a!=="scrypt"||!s||!h)return false; try{const expected=Buffer.from(h,"base64url"); const actual=await scrypt(password,Buffer.from(s,"base64url"),expected.length) as Buffer; return expected.length===actual.length&&timingSafeEqual(expected,actual)}catch{return false}}
