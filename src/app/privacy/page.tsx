import { InfoPage } from "@/components/marketing/InfoPage";
export default function Page(){return <InfoPage title="Privacy by minimum necessary collection." intro="The production system will store account, enrollment, progress, submission, payment-reference, and support data required to deliver training." sections={[["Data use","Progress supports scoring, certificates, instructor review, and learner support."],["Payments","Payment providers process credentials; the academy stores provider references and status records."],["Access","Administrative access is role-restricted and audited."]]}/>}
