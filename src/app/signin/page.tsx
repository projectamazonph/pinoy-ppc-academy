import{Brand}from"@/components/brand/Brand";import{AuthForm}from"@/components/auth/AuthForm";import{signInAction}from"@/app/actions/auth";import{safeReturnTo}from"@/modules/auth/application/session-navigation";export default async function Page({searchParams}:{searchParams:Promise<{returnTo?:string}>}){const p=await searchParams;return<main className="simple-page"><section><Brand/><p className="eyebrow">Student sign in</p><h1>Continue your learning route.</h1><p>Use your Pinoy PPC Academy account. Sessions are stored server-side and revoked when you sign out.</p><AuthForm mode="signin" action={signInAction} returnTo={safeReturnTo(p.returnTo)}/></section></main>}
