import{Brand}from"@/components/brand/Brand";import{AuthForm}from"@/components/auth/AuthForm";import{signUpAction}from"@/app/actions/auth";import{safeReturnTo}from"@/modules/auth/application/session-navigation";export default async function Page({searchParams}:{searchParams:Promise<{returnTo?:string;path?:string}>}){const p=await searchParams,r=p.returnTo?safeReturnTo(p.returnTo):p.path==="va-career-starter"?"/app/courses/va-career-starter":"/app";return<main className="simple-page"><section><Brand/><p className="eyebrow">Free learner account</p><h1>Save your route and start learning.</h1><p>Create an account to keep lesson progress and simulator attempts. The VA Career Starter route remains free.</p><AuthForm mode="signup" action={signUpAction} returnTo={r}/></section></main>}
