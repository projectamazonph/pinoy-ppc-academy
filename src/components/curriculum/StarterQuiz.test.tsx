import{render,screen}from"@testing-library/react";import{it,expect,vi}from"vitest";import{StarterQuiz}from"./StarterQuiz";it("renders sanitized questions",()=>{render(<StarterQuiz completed={false} action={vi.fn(async()=>({status:"idle" as const}))} quiz={{lessonId:"x",version:1,passPercent:80,questions:[{id:"q",prompt:"Safe action?",choices:[{id:"a",label:"Ask"}]}]}}/>);expect(screen.getByRole("radio",{name:"Ask"})).toBeInTheDocument()});
