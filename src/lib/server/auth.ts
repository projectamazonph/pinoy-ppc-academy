import{cookies}from"next/headers";import{redirect}from"next/navigation";import type{PublicUser}from"@/modules/auth/domain/auth";import{getAuthService}from"@/modules/auth/infrastructure/auth-runtime";export const SESSION_COOKIE_NAME="pinoy_ppc_session";export async function getCurrentUser():Promise<PublicUser|null>{const c=await cookies();return getAuthService().authenticate(c.get(SESSION_COOKIE_NAME)?.value)}export async function requireCurrentUser(){const u=await getCurrentUser();if(!u)redirect("/signin?returnTo=/app");return u}export async function setSessionCookie(t:string,e:Date){const c=await cookies();c.set(SESSION_COOKIE_NAME,t,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",expires:e})}export async function clearSessionCookie(){const c=await cookies();c.set(SESSION_COOKIE_NAME,"",{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",expires:new Date(0)})}export async function readSessionToken(){const c=await cookies();return c.get(SESSION_COOKIE_NAME)?.value??null}
